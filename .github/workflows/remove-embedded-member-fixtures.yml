name: Remove Embedded Member Fixtures

on:
  push:
    branches:
      - architecture-stabilization

permissions:
  contents: write

jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: architecture-stabilization
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Remove embedded member fixtures
        run: node scripts/remove-embedded-member-fixtures.js
      - name: Commit source cleanup when changed
        run: |
          if git diff --quiet; then
            echo "Embedded member fixtures already removed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add script.js
          git commit -m "refactor: remove embedded member fixtures"
          git push origin HEAD:architecture-stabilization
