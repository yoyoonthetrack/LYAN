name: V2 Source Migration (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "V2 source migration completed on architecture-stabilization."
          echo "The migration runner is intentionally retired to prevent repeated source rewrites."
