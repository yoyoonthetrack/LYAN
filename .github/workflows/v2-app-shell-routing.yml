name: V2 App Shell Routing Migration (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "App-shell routing migration completed."
          echo "The migration runner is retired so navigation ownership stays explicit and deterministic."
