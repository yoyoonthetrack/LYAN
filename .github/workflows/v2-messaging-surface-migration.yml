name: V2 Messaging Surface Migration (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Messaging surface ownership migration completed."
          echo "The runner is retired so source ownership remains explicit and deterministic."
